# -*- mode: sh -*-
alias la='ls -aG'
alias ll='ls -lG'

alias be="bundle exec"

case ${OSTYPE} in
    darwin*)
        # Emacs
        alias emacs="/Applications/Emacs.app/Contents/MacOS/Emacs -nw"
esac
